# Add A Projection Or Snapshot

1. Put persistence behavior behind `codestory-store`.
2. Keep invalidation and refresh logic in the store layer.
3. Document whether the data is primary or derived.
4. Add store-level tests for invalidation and readback.
5. Update the store subsystem guide and invariants doc.
