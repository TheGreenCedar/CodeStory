interface:
  display_name: "CodeStory Grounding"
  short_description: "Ground local repos through CodeStory MCP."
  default_prompt: "Read and follow the loaded codestory-grounding skill. It is the sole source of truth for CodeStory tool routing and evidence boundaries; this metadata adds no parallel instructions."
dependencies:
  tools:
    - type: "mcp"
      value: "codestory"
      description: "CodeStory plugin MCP server for local repository grounding, status, graph navigation, packets, and search."
      transport: "stdio"
policy:
  allow_implicit_invocation: true
