interface:
  display_name: "Codestory Grounding"
  short_description: "Build the codestory-cli release binary when needed, then query the workspace directly through the release executable."
  default_prompt: "Use $codestory-grounding to build codestory-cli when needed, refresh the index deliberately, and inspect a workspace with direct codestory-cli grounding commands before answering or editing."
