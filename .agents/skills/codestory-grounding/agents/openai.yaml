interface:
  display_name: "CodeStory Grounding"
  short_description: "Use the global CodeStory skill to set up codestory-cli once, then query explicit target workspaces."
  default_prompt: "Use $codestory-grounding as the global CodeStory surface for this agent: resolve or set up codestory-cli once, set an explicit target workspace, refresh the index deliberately, and inspect the workspace with direct grounding commands before answering or editing."
